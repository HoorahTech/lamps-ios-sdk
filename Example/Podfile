use_frameworks! :linkage => :static

platform :ios, '12.0'

target 'LampsSDK_Example' do
  # 静态链接，避免 Ads-CN 等静态 xcframework 与动态 Framework 冲突
  pod 'LampsSDK', :path => '../'

  target 'LampsSDK_Tests' do
    inherit! :search_paths
  end
end
